<h1>Editing Meeting</h1>

<%= form_for :meeting, url: meeting_path(@meeting), method: :patch do |f|%>
  <%= render 'form', meeting: @meeting, f: f %>
<% end %>

<%= link_to 'Back', meeting_path(@meeting) %>
